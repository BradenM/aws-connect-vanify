{
  "name": "aws-connect-vanify",
  "version": "1.0.0",
  "description": "AWS Connect Vanity Number Example",
  "scripts": {
      "deploy:frontend": "pushd frontend && sls deploy && popd",
      "deploy:backend": "pushd backend && sls deploy && popd",
      "update:backend:vanify": "pushd backend && sls deploy -f vanify && popd",
      "update:backend:recent": "pushd backend && sls deploy -f recent && popd"
  },
  "engines": {
    "node": ">=14.15.0"
  },
  "dependencies": {},
  "devDependencies": {
    "serverless": "^2.23.0",
    "serverless-plugin-scripts": "^1.0.2"
  },
  "author": "Braden Mars <bradenmars@bradenmars.me>",
  "license": "MIT"
}
